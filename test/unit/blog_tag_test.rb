require 'test_helper'

class BlogTagTest < ActiveSupport::TestCase
  # Replace this with your real tests.
  test "the truth" do
    assert true
  end

  test "create tag" do
    #tag = BlogTag.new(:one)
    #assert !tag.save
  end
end
