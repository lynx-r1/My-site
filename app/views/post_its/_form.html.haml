= form_for @post_it do |f|
  -if @post_it.errors.any?
    #errorExplanation
      %h2= "#{pluralize(@post_it.errors.count, "error")} prohibited this post_it from being saved:"
      %ul
        - @post_it.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_area :content
  .actions
    = f.submit 'Save'
