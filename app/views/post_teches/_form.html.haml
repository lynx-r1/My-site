= form_for @post_tech do |f|
  -if @post_tech.errors.any?
    #errorExplanation
      %h2= "#{pluralize(@post_tech.errors.count, "error")} prohibited this post_tech from being saved:"
      %ul
        - @post_tech.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_area :content
  .actions
    = f.submit 'Save'
